#Find the nth term 5,13,25,41,61,.........................
n = int(input("Enter the value of n: "))

# Starting terms of the sequence
first_term = 5  # The first term of the sequence
difference = 8  # The difference between consecutive terms

# We can directly generate the nth term using the pattern observed.
term = first_term
for i in range(1, n):
    term += difference
    difference += 4  # The difference increases by 4 at each step

print("The nth term is:", term)
